# AI 助教 RAG：擇一設定即可（有 HUGGINGFACE_API_KEY 時優先使用 HF）

# Hugging Face Inference API（推薦，使用現成模型如 Llama 3.2）
# 請至 https://huggingface.co/settings/tokens 建立具 Inference 權限的 token
HUGGINGFACE_API_KEY=
# 可選，預設為 meta-llama/Llama-3.2-3B-Instruct
HF_MODEL=meta-llama/Llama-3.2-3B-Instruct

# OpenAI API（未設定 HUGGINGFACE_API_KEY 時使用）
# OPENAI_API_KEY=

# NextAuth
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
# Required for local dev to avoid ERR_TOO_MANY_REDIRECTS. Use http (not https), no trailing slash.
NEXTAUTH_URL=http://localhost:3000

# Google OAuth — https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth — https://github.com/settings/developers → OAuth Apps
GITHUB_ID=
GITHUB_SECRET=
